var PARAMS = {
    // world
    worldWidth: 800,
    worldHeight: 800,
    
    // bands
    numBands: 20,
    bandSize: 50,

    // game engine
    updatesPerDraw: 1,

    // database
    db: "warcryDB",
    collection: "test"
};

function loadParameters() {

    console.log(PARAMS);
};

const runs = [

];
